# backend/ — Express API + email worker

This folder will hold:
- The Express API (routes the frontend calls)
- The email-sending worker (pg-boss queue → Amazon SES)
- `migrations/` — SQL that creates the database tables

We build this starting in **Step 3**.
